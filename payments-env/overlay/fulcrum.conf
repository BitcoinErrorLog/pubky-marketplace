# Fulcrum configuration for the composed local regtest environment.
# Local-only credentials; they never leave the compose network.
datadir = /data
bitcoind = bitcoind:18443
rpcuser = paykitregtest
rpcpassword = paykitregtestpass
tcp = 0.0.0.0:50001
peering = false
announce = false
